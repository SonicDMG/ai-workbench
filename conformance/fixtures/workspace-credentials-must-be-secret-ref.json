{
	"slug": "workspace-credentials-must-be-secret-ref",
	"description": "Raw credential values are rejected with 400 validation_error before they can reach the SecretResolver.",
	"captures": [
		{
			"request": {
				"body": {
					"credentials": {
						"token": "AstraCS:plaintext"
					},
					"kind": "astra",
					"name": "w"
				},
				"method": "POST",
				"path": "/api/v1/workspaces"
			},
			"response": {
				"body": {
					"error": {
						"code": "validation_error",
						"docs": "docs/errors.md#validation-error",
						"hint": "Request body or query string failed schema validation; see the message for the offending field.",
						"message": "credentials.token: expected '<provider>:<path>', e.g. 'env:FOO'",
						"requestId": "{{REQID_1}}"
					}
				},
				"status": 400
			},
			"step": 1
		}
	]
}
